#include "catch.hpp"
#include "../metier/Position.h"
#include "../metier/Piece.h"
#include "../metier/Player.h"
#include "../metier/Game.h"
#include "../metier/Color.h"
//namespace std {


/*TEST_CASE("Testing move")
{

    SECTION("verify that given position has move ")
    {
        vector<Piece> a;
        a.push_back(Piece(ROLE::SPY,Position(5,1)));
    a.at(0).movePiece(Position(4,1));
bool except=a.at(0).getPiecePosition().equals(Position(5,1));
        REQUIRE(except==false);
              }

    SECTION("Testing move it's not possible")
    {
        vector<Piece> a;
        a.push_back(Piece(ROLE::SPY,Position(6,3)));
    a.at(0).movePiece(Position(5,3));
bool except=a.at(0).getPiecePosition().equals(Position(6,3));
        REQUIRE(except==true);
         }
}
*/

/*TEST_CASE("Testing increase Laster Position")
{

}*/
//}
